# turtle_mission 的航点参数
# 顶层键必须是节点名（waypoint_follower），参数在 ros__parameters 下
# waypoints 是 "标签,x坐标,y坐标" 字符串数组，由 waypoint_follower 解析
waypoint_follower:
  ros__parameters:
    waypoints: ["A,2.0,2.0", "B,8.0,8.0", "C,8.0,2.0", "D,2.0,8.0"]
    tolerance: 0.2
    max_speed: 2.0
    wait_between: 0.5
